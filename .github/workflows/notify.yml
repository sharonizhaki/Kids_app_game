name: Scheduled Parent Notifications (disabled)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled — notifications are handled by Firebase Cloud Functions'
        required: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "disabled"
